# lms-rest-api documentation

> Generated by compodoc 2.0.0 at 2026-08-11T05:54:05.510Z, llm-md export.

## Modules

### AppModule

File: `src/app/app.module.ts`
providers: `AppService`
imports: `AuthModule`, `PostsModule`, `UsersModule`

### AuthModule

File: `src/auth/auth.module.ts`
providers: `AuthService`
imports: `UsersModule`

### PostsModule

File: `src/posts/posts.module.ts`
providers: `PostsService`
exports: `PostsService`

### UsersModule

File: `src/users/users.module.ts`
providers: `UsersService`
exports: `UsersService`

## Services / Injectables

### AppService

File: `src/app/app.service.ts`

Description: This is the App service

Methods:
- `getHello(): string` — This is the Hello service function

### AuthService

File: `src/auth/auth.service.ts`

Description: This is the Auth service

Methods:
- `create(): any`
- `findAll(findAllDto: FindAllDto): any`
- `findOne(findOneDto: FindOneDto): any`
- `login(loginDto: LoginDto): { status: string; message: string; payload: LoginDto; }`
- `register(registerDto: RegisterDto): { status: string; message: string; payload: RegisterDto; }`

### PostsService

File: `src/posts/posts.service.ts`

Description: This is the Posts service

Methods:
- `createPost(createPostDto: CreatePostDto): { message: string; createPostDto: CreatePostDto; }`
- `findAllPostsOfUser(): string`
- `findPosts(userId: string): string`
- `patchPost(patchPostDto: PatchPostDto): { message: string; patchPostDto: PatchPostDto; }`

### UsersService

File: `src/users/users.service.ts`

Description: This is the Users service

Methods:
- `create(): { status: string; message: string; }`
- `findAll(findAllDto: FindAllDto): { status: string; message: string; findAllDto: FindAllDto; }`
- `findAllPostsOfUser(): any`
- `findOne(findOneDto: FindOneDto): { status: string; message: string; }`
- `findOneForPost(userId: string): boolean`
- `getKeys(): { status: string; message: string; databaseConfig: any; serverConfig: any; }`

## Classes

### CreatePostDto

File: `src/posts/dtos/create-post.dto.ts`

Description: This is DTO for creating a post

Properties:
- `content: string`
- `postStatus: PostStatusEnum`
- `postType: PostTypeEnum`
- `schema: string`
- `slug: string`
- `tags: string[]`
- `thumbnailUrl: string`
- `title: string`

### FindAllDto

File: `src/users/users.dto.ts`

Description: This is DTO for finding all the users

Properties:
- `category: string` — Category to filter
- `minPrice: number` — Min price to filter

### FindOneDto

File: `src/users/users.dto.ts`

Description: This is DTO for finding a specific user

Properties:
- `id: number` — Id of the user

### LoginDto

File: `src/auth/auth.dto.ts`

Description: This is DTO for auth login

Properties:
- `email: string` — Email
- `password: string` — Password

### PatchPostDto

File: `src/posts/dtos/patch-post.dto.ts`
Extends: `PartialType(CreatePostDto)`

Description: This is DTO for updating a post

Properties:
- `id: number` — Id of the user

### RegisterDto

File: `src/auth/auth.dto.ts`

Description: This is DTO for auth registration

Properties:
- `age?: number` — Age
- `email: string` — Email
- `password: string` — Password
- `sex?: string` — Sex
- `username: string` — Username

## Interfaces

### DatabaseConfig

File: `src/config/database_config.interface.ts`

Description: This is Interface for Database config

Properties:
- `pass: string` — The database password
- `port: string` — The database port
- `user: string` — The database username

### ServerConfig

File: `src/config/server_config.interface.ts`

Description: This is Interface for Server config

Properties:
- `port: string` — The port where the server will run

## Public functions

- `bootstrap()`
  Defined in `src/main.ts`

## Public enumerations

- `enum PostStatusEnum`
  - `DRAFT = draft`
  - `REVIEW = review`
  - `SCHEDULED = scheduled`

- `enum PostTypeEnum`
  - `POST = post`
  - `PAGE = page`
  - `STORY = story`
